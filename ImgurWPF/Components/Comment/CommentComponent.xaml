<UserControl
    x:Class="ImgurWPF.Components.Comment.CommentComponent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ImgurWPF.Components.Comment"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vote="clr-namespace:ImgurWPF.Components.Vote"
    x:Name="Root"
    d:DesignHeight="150"
    d:DesignWidth="400"
    mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Style x:Key="MoreCommentsButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Background" Value="#F5F7FA" />
            <Setter Property="Foreground" Value="#334155" />
            <Setter Property="BorderBrush" Value="#E2E8F0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="14"
                            SnapsToDevicePixels="True">
                            <ContentPresenter
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#EAF2FF" />
                                <Setter Property="Foreground" Value="#1D4ED8" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="#BFDBFE" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#DBEAFE" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="#93C5FD" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ReplyTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="#1E293B" />
            <Setter Property="BorderBrush" Value="#D8E1EC" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border
                            x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="12"
                            SnapsToDevicePixels="True">
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Margin="0"
                                VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#BFDBFE" />
                                <Setter TargetName="Border" Property="Background" Value="#FCFDFF" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#60A5FA" />
                                <Setter TargetName="Border" Property="Background" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#F8FAFC" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="#E2E8F0" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel Margin="12">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                FontWeight="SemiBold"
                Text="{Binding Author}" />

            <TextBlock
                Grid.Row="1"
                Margin="0,4,0,8"
                Text="{Binding Comment}"
                TextWrapping="Wrap" />

            <vote:VoteComponent
                Grid.Row="2"
                Command="{Binding VoteCommand}"
                Score="{Binding Score}"
                Vote="{Binding Vote}" />

            <StackPanel
                Grid.Row="3"
                Margin="0,8,0,0"
                Orientation="Horizontal">
                <Button Margin="0,0,8,0" Command="{Binding ToggleReplyCommand}">
                    <Button.Style>
                        <Style BasedOn="{StaticResource MoreCommentsButtonStyle}" TargetType="Button">
                            <Setter Property="Content" Value="回覆" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsReplyExpanded}" Value="True">
                                    <Setter Property="Content" Value="取消" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Command="{Binding ToggleChildrenCommand}" Visibility="{Binding HasChildren, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Button.Style>
                        <Style BasedOn="{StaticResource MoreCommentsButtonStyle}" TargetType="Button">
                            <Setter Property="Content" Value="更多留言" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChildrenExpanded}" Value="True">
                                    <Setter Property="Content" Value="隱藏" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

            <Border
                Grid.Row="4"
                Margin="0,8,0,0"
                Padding="10"
                Background="#F8FAFC"
                BorderBrush="#E2E8F0"
                BorderThickness="1"
                CornerRadius="10"
                Visibility="{Binding IsReplyExpanded, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel>
                    <TextBox
                        MinHeight="72"
                        AcceptsReturn="True"
                        Style="{StaticResource ReplyTextBoxStyle}"
                        Text="{Binding ReplyInput, UpdateSourceTrigger=PropertyChanged}"
                        TextWrapping="Wrap"
                        VerticalScrollBarVisibility="Auto" />
                    <Button
                        Margin="0,8,0,0"
                        HorizontalAlignment="Right"
                        Command="{Binding SubmitReplyCommand}">
                        <Button.Style>
                            <Style BasedOn="{StaticResource MoreCommentsButtonStyle}" TargetType="Button">
                                <Setter Property="Content" Value="送出" />
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>

        <ItemsControl
            x:Name="container"
            Margin="20,8,0,0"
            ItemsSource="{Binding Children}"
            Visibility="{Binding IsChildrenExpanded, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Margin" Value="0,0,0,8" />
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:CommentComponent />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>